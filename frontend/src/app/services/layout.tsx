export const metadata = {
  title: 'Services - Wiwihood',
  description: 'Browse and book services from verified providers',
}

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {children}
    </>
  )
}
